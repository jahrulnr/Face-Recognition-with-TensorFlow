<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FaceRecognition" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/bootstrap_gray_dark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="textview" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/bootstrap_gray_lighter</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="edittext" parent="Widget.AppCompat.EditText">
        <item name="android:maxLines">1</item>
        <item name="android:singleLine">true</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="android:textColor">@color/bootstrap_gray_lightest</item>
        <item name="android:textColorHint">@color/bootstrap_gray_lighter</item>
        <item name="android:backgroundTint">@color/bootstrap_brand_primary</item>
    </style>

    <style name="button" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/bootstrap_gray_lighter</item>
        <item name="android:backgroundTint">@color/bootstrap_brand_primary</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>